apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-pvc-block
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeMode: Block
  resources:
    requests:
      storage: 500M
